<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit <%= product.name %> — Daily Dose</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet" />
  <style>
    :root{ --primary:#2d2a26; --accent:#c49a6c; --bg:#faf7f2; --ink:#1e1c19; --line:#e9e4da }
    *{margin:0;padding:0;box-sizing:border-box} body{font-family:"Poppins";color:var(--ink);background:var(--bg)}
    h1,h2,h3{font-family:"Playfair Display",serif;color:var(--primary)} a{text-decoration:none;color:inherit}
    .navbar{position:fixed;top:0;left:0;right:0;height:68px;display:flex;align-items:center;justify-content:space-between;padding:0 7%;background:rgba(250,247,242,.85);border-bottom:1px solid var(--line)}
    .navbar-logo a{font-family:"Playfair Display",serif;font-weight:700;font-size:1.6rem;color:var(--primary)} .navbar-nav{list-style:none;display:flex;gap:1.2rem}
    .nav-spacer{height:68px}
    main.section{padding:3rem 7%}
    .container{max-width:640px;margin:0 auto}
    .form-card{background:#fff;border:1px solid var(--line);border-radius:16px;padding:2rem}
    .alert{padding:.75rem 1rem;border-radius:10px;margin:.75rem 0}
    .alert-error{background:#fff2f2;border:1px solid #ffd6d6;color:#b42318}
    .error-list{margin-left:1rem}
    .form-grid{display:grid;gap:.9rem}
    label{font-weight:600;color:var(--primary)}
    input,textarea{border:1px solid var(--line);border-radius:10px;padding:.7rem .9rem;background:#fff;color:var(--ink)}
    textarea{min-height:120px;resize:vertical}
    input:focus,textarea:focus{outline:2px solid var(--accent);outline-offset:2px}
    .btn{display:inline-block;padding:.9rem 1.6rem;border-radius:999px;border:1px solid transparent;background:var(--primary);color:#fff;font-weight:600}
    .btn:hover{opacity:.96;transform:translateY(-1px)}
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-logo"><a href="/">Daily Dose Coffee</a></div>
    <ul class="navbar-nav">
      <li><a href="/">Home</a></li>
      <% if (currentUser) { %>
        <li><a href="/products">Products</a></li>
        <li><a href="/categories">Categories</a></li>
        <li><a href="/cart">Cart</a></li>
        <% if (currentUser.role === 'admin') { %><li><a href="/products/add">Add Product</a></li><% } %>
        <li><a href="/profile">Profile</a></li>
        <li><a href="/logout">Logout</a></li>
      <% } else { %>
        <li><a href="/login">Login</a></li>
        <li><a href="/register">Register</a></li>
      <% } %>
    </ul>
  </nav>
  <div class="nav-spacer"></div>

  <main class="section">
    <div class="container">
      <div class="form-card">
        <h1>Edit Bean</h1>
        <% if (errors && errors.length) { %>
          <div class="alert alert-error"><ul class="error-list"><% errors.forEach(m=>{ %><li><%= m %></li><% }) %></ul></div>
        <% } %>
        <form action="/products/<%= product.id %>/edit" method="post" class="form-grid" novalidate>
          <label for="name">Name</label>
          <input id="name" name="name" value="<%= product.name %>" required>

          <label for="description">Description</label>
          <textarea id="description" name="description" required><%= product.description %></textarea>

          <label for="price">Price</label>
          <input id="price" name="price" type="number" min="1" value="<%= product.price %>" required>

          <label for="stock">Stock</label>
          <input id="stock" name="stock" type="number" min="0" value="<%= product.stock %>" required>

          <label for="imageURL">Image URL</label>
          <input id="imageURL" name="imageURL" type="url" value="<%= product.imageURL %>" required>

          <button type="submit" class="btn">Save</button>
        </form>
      </div>
    </div>
  </main>
</body>
</html>